import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Account",
  description: "User details, Update, Delete",
};

const Account = () => {
  return <div>Account</div>;
};

export default Account;
