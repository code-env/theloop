import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Inbox",
  description: "User Messages, check if someone has reached out to you!",
};

const Inbox = () => {
  return <div>Inbox</div>;
};

export default Inbox;
