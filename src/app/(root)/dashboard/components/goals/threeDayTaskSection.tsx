// import { DayData, userGoals } from "@/lib/types";
// import React, { FC } from "react";
// import Task from "./Task";

// interface DayTaskProps {
//   goals: userGoals;
// }

// const ThreeDayTaskSection: FC<DayTaskProps> = ({ goals }) => {
//   return (
//     <section className="flex items-start gap-5">
//       {goals.map((goal: DayData, index) => (
//         <Task key={index} task={goal} />
//       ))}
//     </section>
//   );
// };

// export default ThreeDayTaskSection;
